注意：

实操题，没有标准答案，凡是相对合理的作答，都可得分；


深度学习（5个）：

1，搭建一个可用于波士顿房价预测的全连接网络模型（可借助 PyTorch）；
2，搭建一个可用于手写数字识别的卷积网络模型（黑白图像，28*28大小，10个分类；可借助 PyTorch）；
3，搭建一个可用于垃圾短信分类的循环神经网络模型（短信最长70个字，可借助 PyTorch）；
4，对指定语料（corpus文件夹下所有内容）构建字典（可借助 jieba 分词）；
5，设计一个可用于计算机视觉任务的数据增强处理（可借助 torchvision）；